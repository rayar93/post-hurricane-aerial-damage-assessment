# UAV preprocessing figures

Store the visual evidence for each experiment in this directory.

Required primary filename:

```text
<STEP-ID>_visual-comparison.png
```

Optional supporting files:

```text
<STEP-ID>_metric-comparison.png
<STEP-ID>_confusion-matrix.png
```

Use the same source samples across variants and include representative damage classes and failure modes.
